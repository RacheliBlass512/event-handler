// Skeleton service worker stub (Mode B / Web Push) — skeleton-plan.md decision: thin stub only.
// TODO: handle 'push' events and deep-link navigation on notificationclick once Web Push is
// actually implemented.

self.addEventListener('push', () => {
  // TODO: show a notification built from the push payload.
});

self.addEventListener('notificationclick', () => {
  // TODO: navigate to the relevant event screen (deep link).
});
